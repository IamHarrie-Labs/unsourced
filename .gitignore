node_modules/
dist/
*.log
.DS_Store

# wallet secrets and sync caches, never commit these
.midnight-state.json
.midnight-wallet-state/
midnight-level-db/
.survey-roster-keys.*.json

# local-only env overrides: .env.production stays tracked, it's just a
# public network id + public contract address, no secrets
.env.local

.vercel
